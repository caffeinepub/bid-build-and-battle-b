{
  "frontend/src/pages/WatchPage.tsx": "Public read-only live auction viewer; imports `useCurrentPlayer`, `useCurrentAuctionState`, and `AuctionStatus` from `useQueries` (all now properly exported via aliases and re-exports).",
  "frontend/src/pages/ResultsPage.tsx": "Auction results page with summary stats, tabbed teams/players views, CSV/JSON export, and role filter buttons; fixes the invalid `variant=\"grid\"` prop on `SkeletonLoader` by changing it to the supported `variant=\"card\"`.",
  "frontend/src/pages/AdminLogin.tsx": "Admin login page with passcode form that calls `adminLogin.mutateAsync` and navigates to the dashboard on success; includes a `getFriendlyErrorMessage` helper that intercepts raw IC/canister errors (IC0508, replica rejections, canister-stopped messages) and replaces them with clean, user-friendly messages that hide internal canister IDs and request IDs.",
  "frontend/src/components/ProtectedRoute.tsx": "Route guard component using `useIsAdmin` and `useIsCallerApproved` from `useQueries` (both now properly exported) for admin and approval-based route protection.",
  "frontend/src/hooks/useQueries.ts": "Complete React Query hooks file for all backend interactions; the `useAdminLogin` mutation stores `adminLoggedIn` in localStorage after successful passcode login, and `useAddPlayer` uses the authenticated actor directly.",
  "frontend/src/pages/AdminDashboard.tsx": "Full admin dashboard with Players/Teams/Auction/Approvals tabs; the `AddPlayerForm` uses `useActor` directly to verify actor availability before submission, with clear error messages and inline loading states.",
  "frontend/src/components/AppHeader.tsx": "App header with navigation links and auth buttons; separates admin logout (passcode-based, clears query cache only) from team logout (clears Internet Identity + cache), and shows the correct logout button based on admin vs. team member status.",
  "frontend/src/components/PlayerCard.tsx": "Reusable player card component that imports `PlayerRole` and `PlayerCategory` directly from `../backend` instead of `../hooks/useQueries` to fix the re-export error.",
  "frontend/src/pages/TeamDashboard.tsx": "Team owner dashboard showing the live auction player, bid controls, budget tracker, and squad view; imports `useCurrentPlayer`, `useCurrentAuctionState`, `useGetCallerUserProfile`, `AuctionStatus`, and `PlayerCategory` from `useQueries` (all now properly exported).",
  "frontend/src/pages/TeamLogin.tsx": "Team login page using Internet Identity with approval flow; imports `useIsCallerApproved`, `useIsCallerAdmin`, `useGetCallerUserProfile`, and `useRequestApproval` from `useQueries` (all now properly exported).",
  "frontend/src/pages/TeamRegister.tsx": "Team registration page with form validation; imports `useRegisterTeam`, `useGetCallerUserProfile`, and `useSaveCallerUserProfile` from `useQueries` (all now properly exported).",
  "frontend/src/hooks/useActor.ts": "Custom hook that creates and caches the backend actor using React Query; sets `gcTime: Infinity` to prevent actor garbage collection across navigation, uses a `useRef` to track actor changes and only invalidate non-actor queries when the actor instance actually changes."
}